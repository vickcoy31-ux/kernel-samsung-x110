name: Build Samsung X110 Kernel with KernelSU

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libncurses-dev bison flex libssl-dev libelf-dev bc git wget curl ccache rsync kmod cpio python3 lz4

      - name: Download Clang Toolchain
        run: |
          echo "Mengunduh Proton-Clang Compiler..."
          git clone --depth=1 https://github.com/kdrag0n/proton-clang.git clang

      - name: Clone Source Code
        run: |
          echo "Mengunduh Source Code Kernel Samsung GTA9..."
          git clone --depth=1 https://github.com/0xA11CE613/kernel_samsung_gta9.git kernel_source

      - name: Inject KernelSU Clean Setup
        run: |
          echo "=== 1. Masuk ke folder kernel dan bersihkan KSU lama ==="
          cd kernel_source || exit 1
          rm -rf drivers/kernelsu
          
          echo "=== 2. Mengunduh KernelSU tepat di drivers/kernelsu ==="
          git clone --depth=1 https://github.com/tiann/KernelSU.git drivers/kernelsu
          
          echo "=== 3. Membersihkan dan memasukkan konfigurasi Kconfig ==="
          sed -i '/kernelsu/d' drivers/Kconfig
          echo 'source "drivers/kernelsu/Kconfig"' >> drivers/Kconfig
          
          echo "=== 4. Membersihkan dan memasukkan konfigurasi Makefile ==="
          sed -i '/kernelsu/d' drivers/Makefile
          echo 'obj-y += kernelsu/' >> drivers/Makefile

      - name: Start Full Compilation
        run: |
          cd kernel_source || exit 1
          
          # Setup Environment Clang & Cross Compiler
          export ARCH=arm64
          export SUBARCH=arm64
          export PATH="$(pwd)/../clang/bin:$PATH"
          export CC=clang
          export CROSS_COMPILE=aarch64-linux-gnu-
          export CROSS_COMPILE_ARM32=arm-linux-gnueabi-
          
          echo "=== Mencari file defconfig GTA9/X110 ==="
          CONFIG_PATH=$(find arch/arm64/configs/ -type f \( -iname "*gta9*" -o -iname "*x110*" \) | head -n 1)
          
          if [ -n "$CONFIG_PATH" ]; then
            TARGET_CONFIG=$(echo "$CONFIG_PATH" | sed 's|arch/arm64/configs/||')
            echo "Memakai config: $TARGET_CONFIG"
            make CC=clang $TARGET_CONFIG
          else
            echo "Memakai defconfig fallback..."
            make CC=clang defconfig
          fi
          
          echo "Memastikan opsi KernelSU aktif di .config..."
          chmod +x scripts/config || true
          scripts/config --enable CONFIG_KSU || echo "CONFIG_KSU=y" >> .config
          
          echo "Memproses kompilasi kernel dengan KernelSU..."
          make -j$(nproc) CC=clang Image

      - name: Upload Final Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: samsung-x110-kernelsu-kernel
          path: kernel_source/arch/arm64/boot/Image
